import 'package:flutter/material.dart';

class PostDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('博客细节页'),
      ),
      body: Container(
        child: Center(child: Text('我是正文哈哈哈哈正文')),
      ),
    );
  }
}
